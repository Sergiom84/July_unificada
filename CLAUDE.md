# Instrucciones para Claude

Este repositorio es la memoria procedimental local de Sergio.

La fuente de instrucciones principal vive en:

`AGENTS.md`

Antes de actuar sobre este repositorio, Claude debe leer `AGENTS.md` y seguir sus reglas:

- mantener `context/raw/` como fuente bruta;
- mantener `context/wiki/` como wiki activa;
- actualizar `context/wiki/index.md` y `context/wiki/log.md` después de cambios relevantes;
- escribir en español correcto, con tildes y ortografía cuidada;
- no subir ni sincronizar información con servicios externos salvo petición explícita;
- no guardar secretos, claves ni valores crudos de `.env`.

Regla práctica:

July recuerda la sesión. La wiki de `July_unificada` conserva el criterio.
